version: "3.9"

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: StravaAutoKudos
    command: python main.py
    volumes:
      - .:/usr/src/app
    env_file:
      - ./.env